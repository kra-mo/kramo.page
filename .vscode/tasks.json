{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Jekyll serve toolbox",
            "type": "shell",
            "command": "xdg-open http://127.0.0.1:4000 && toolbox run bundle exec jekyll serve --livereload",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "Jekyll serve host",
            "type": "shell",
            "command": "xdg-open http://127.0.0.1:4000 && bundle exec jekyll serve --livereload",
            "problemMatcher": [],
            "group": {
                "kind": "build"
            }
        }
    ]
}